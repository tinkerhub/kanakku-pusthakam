# Python / Django
__pycache__/
*.py[cod]
.venv/
venv/
*.sqlite3
backend/.env
# Root deployment env written by setup.sh / setup.ps1 (holds generated secrets).
/.env
staticfiles/

# Node / Vite
node_modules/
frontend/dist/
frontend/.env

# tsc -b composite build artifacts (emitted from tsconfig.node.json)
frontend/vite.config.js
frontend/vite.config.d.ts
*.tsbuildinfo

# Codex / agent scratch prompts + orchestration artifacts
.codex-prompt-*.txt
.cbrand*.txt
.cplan.md

# Internal planning + design docs — kept local, not published to the repo.
# Only curated user/ops docs are committed (e.g. docs/setup-for-makerspaces.md,
# docs/self-hosting.md).
docs/prd-*.md
docs/PLAN-prd-*.md
docs/superpowers/
docs/design/
docs/plans/
docs/HANDOVER.md
docs/roadmap.md

# OS / editor
.DS_Store
.idea/
.vscode/

# Stitch design mockups (reference only, not consumed by Vite; CDN refs violate CSP)
stitch_luminous_minimalist_interface/

# Local Postgres backup/migration artifacts — never commit (may contain user/secret/tenant data)
*.dump
